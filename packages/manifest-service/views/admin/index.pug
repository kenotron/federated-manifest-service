html
  head
    title title
  body
    h1 Edit the manifest
    form(action='/admin/manifest', method='post')
      div
        textarea(style='width:500px;height:800px;',name='manifest')= manifest
      div
        button(type=submit) save manifest